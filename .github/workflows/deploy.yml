on:
  workflow_run:
    workflows: ["First Workflow"]
    types:
      - success

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps: 
    - name: Deploy
      run: echo "Deploying the project..."
